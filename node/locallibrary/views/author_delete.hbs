<h1>{{title}}: {{author.name}}</h1>
<p>({{author.date_of_birth_formatted}}
  -
  {{author.date_of_death_formatted}})</p>

{{#if author_books.length}}
  <p><strong>Delete the following books before attempting to delete
      this author</strong></p>
  <h3>Books</h3>
  {{#each author_books}}
    <a href={{this.url}}>{{this.title}}</a>
    <p>{{this.summary}}</p>
  {{/each}}
{{else}}
  <p>Do you really want to delete this Author?</p>

  <form method="post" action="">
    <input
      type="hidden"
      name="authorid"
      ,
      required="true"
      ,
      value="{{author._id}}"
    />
    <button type="submit">Delete</button>
  </form>
{{/if}}